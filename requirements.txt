# AI Yoga Coach v1.0 - Dependencies

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# CORS support
python-multipart==0.0.6

# Database (choose one or more)
# SQLite is built into Python, no install needed!
# supabase==2.0.0  # Optional, paid service
pymongo==4.6.0  # For MongoDB Atlas (free tier available)

# Vector Stores (optional - choose one or more)
# pinecone-client==2.2.4
# chromadb==0.4.15
# weaviate-client==3.24.1

# RAG Ingestion
PyPDF2==3.0.1
requests==2.31.0
beautifulsoup4==4.12.2

# LLM Clients for yoga flow (choose one; free/cheap options)
groq==0.4.1
httpx>=0.24.0,<0.28.0  # groq 0.4.x passes 'proxies' to httpx; httpx 0.28+ removed it → Client.__init__() unexpected keyword 'proxies'
google-generativeai==0.3.2
requests>=2.28.0
# Ollama: no extra package, uses requests to http://localhost:11434

# Environment variables
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0
