# Optional: Backend API base URL.
# Leave empty to use Next.js rewrites (proxy from /api to http://localhost:8000/api).
# If backend runs elsewhere, set e.g.:
# NEXT_PUBLIC_API_URL=http://localhost:8000
